version: "3.8"

services:
  vpn-monitor:
    build: .
    container_name: vpn-config-monitor
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./bin:/app/bin
